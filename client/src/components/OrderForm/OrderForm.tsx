import React from 'react';
import './OrderForm.scss'

interface OrderFormProps {
  renderCart?: boolean
}

export const OrderForm = ({ renderCart }: OrderFormProps) => {
  return (
    <>
    
    </>
  );
}

